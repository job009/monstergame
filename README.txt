Client created by Haley Wichman
Runs on python 3.
Must have pygame, setuptools, and pyperclip installed.

To install dependencies, open terminal; then:
>pip install pygame
>pip install setuptools
>pip install pyperclip
